#!/bin/sh

# kill services
#svc -k

# remount root as read only
mount -o remount,ro /

# unmount the rest
umount -a
sync

case $1 in
  reboot) reboot -f ;;
  poweroff) poweroff -f ;;
  *) echo "Invalid action for rc.shutdown" && exit 1 ;;
esac
