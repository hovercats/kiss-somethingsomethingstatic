#!/bin/sh

umask 022

mkdir -p /dev/pts
mkdir -p /dev/shm

# mount stuff
mount -t proc none /proc
mount -o remount,rw /
mount -a

# run local configs
/etc/rc.local

# start service manager
/etc/rc.svc
